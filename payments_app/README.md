# payments_app

Pequeña aplicación de ejemplo en Ruby que implementa clases `Student` y `Account`.

Uso rápido:

```
ruby payments_app/cli.rb
```

Ejecutar pruebas:

```
ruby -Ipayments_app/lib test/test_account.rb
```
